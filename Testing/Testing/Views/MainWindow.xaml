<Window x:Class="Testing.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Testing.ViewModels"
        mc:Ignorable="d"
        Title="MVVM Sample" Width="520" Height="360"
        WindowStartupLocation="CenterScreen">

    <!-- Easiest MVVM wiring: set DataContext to the ViewModel in XAML -->
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="18" FontWeight="SemiBold"
                   Text="MainWindow ↔ ViewModel binding demo" />

        <!-- Two-way binding: user types, VM property updates -->
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center" Text="Name:" Margin="0,0,8,0"/>
            <TextBox Width="220"
                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" >
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter"
                                Command="{Binding AddCommand}" />
                </TextBox.InputBindings>
            </TextBox>
            <Button Margin="12,0,0,0" Padding="12,6"
                    Content="Add"
                    Command="{Binding AddCommand}" />
        </StackPanel>

        <!-- One-way binding: VM computed property -->
        <TextBlock Grid.Row="4" Margin="0,0,0,8"
                   FontStyle="Italic"
                   Text="{Binding StatusMessage}" />

        <!-- ItemsSource binding: VM collection -->
        <Border Grid.Row="4" Margin="0,28,0,0" BorderThickness="1" CornerRadius="6" Padding="8">
            <Border.BorderBrush>
                <SolidColorBrush Color="#33000000"/>
            </Border.BorderBrush>

            <DockPanel>
                <Button DockPanel.Dock="Top" Padding="12,6" Margin="0,0,0,8"
                        Content="Clear"
                        Command="{Binding ClearCommand}" />
                <ListBox ItemsSource="{Binding Names}" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
